/blur/tableLocks/<table name>/<shard id ep node with uuid as data>
/blur/tables/<table name>/<shard id perm node with dir uri as data>
/blur/nodes/<NODE type>/<hostname ep node with null data
/blur/ref/<table name>/<shard id>/<files that are being referenced by a reader>