-if can?(:update, :blur_tables)
  %button.btn.btn-primary{:disabled => 'disabled', :blur_bulk_action => 'disable'} Disable Tables

%table.table.table-striped.table-bordered{:blur_cluster_id => cluster}
  %thead
    %tr
      %th
        %input.bulk-action-checkbox.check-all{:type => "checkbox", :disabled=>'disabled'}
      %th Name
      %th Hosts/Shards
      %th Row Count
      %th Record Count
      %th Info
  %tbody
    %tr.no-tables
      %th
      %th{:colspan=>5} Loading Tables...