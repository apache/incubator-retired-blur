The following tools are included in this repository:

AFS
    - An abstract file system viewer gem

Blur-shell
    - An interactive console tool to run commands directly on blur, similar to an irb for blur

Admin-console
    - The admin rails application for administering a Blur instance
