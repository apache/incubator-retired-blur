development:
  adapter: mysql
  encoding: utf8
  database: blurtools-development
  host: localhost
  pool: 5
  username: root
  password: 

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  encoding: utf8
  database: <%= ENV['BAMBOO'] != 'true' ? 'blurtools-test' : 'blurtools_bamboo' %>
  host: <%= ENV['BAMBOO'] != 'true' ? 'localhost' : 'nic-mysqldev01' %>
  pool: 5
  username: <%= ENV['BAMBOO'] != 'true' ? 'root' : 'blurtools_bamboo' %>
  password: <%= ENV['BAMBOO'] != 'true' ? '' : 'blurtools_bamboo' %>
