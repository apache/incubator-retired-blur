!!!5
%html
  %head
    =csrf_meta_tag
    %title Blur Admin
    = stylesheet_link_tag 'jquery-ui', 'compiled/application'
  %body
    #root
      #hd
        = image_tag 'blur_logo.png'
      #nav
        %ul
          - controller = params[:controller]
          %li{:class => controller == 'main' && 'active'}
            = link_to 'Environment Status', '/'

          %li{:class => controller == 'data' && 'active'}
            = link_to 'Data', data_path
          
          %li{:class => controller == 'runtime' && 'active'}
            = link_to 'Runtime', runtime_path

          %li{:class => controller == 'config' && 'active'}
            = link_to 'Configuration', config_path

          %li{:class => controller == 'queries' && 'active'}
            = link_to 'Query', query_path
      #bd
        = yield
      #root-footer
    #ft 
      Blur Footer
    =javascript_include_tag 'jquery', 'jquery-ui', 'rails', 'application', 'compiled/runtime'
    =include_safe_javascript
